# Replace Nginx Conf
echo "" > /etc/nginx/nginx.conf && nano /etc/nginx/nginx.conf && sudo nginx -t && sudo systemctl restart nginx

# Open DB
sudo -u postgres psql -d tokens

# Monitor Status
journalctl -f -u coinscan_api
journalctl -f -u coinscan_services